spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aliman?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Mugdumpuram@2025
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.format_sql: true
  mail:
    host: localhost
    port: 1025
    username: 'admin@neevinfra.co.uk'
    password: 'Neevinfra@2025'
    properties:
      mail.smtp.auth: false
      mail.smtp.starttls.enable: false

server:
  port: 8090
  forward-headers-strategy: framework

app:
  cors:
    allowed-origins:
      - https://ta.neevinfra.uk
      - https://taapi.neevinfra.uk
      - http://localhost:5090
      - http://localhost:5173

springdoc:
  api-docs.path: /api/docs
  swagger-ui.path: /api/swagger
